# 常见问题

本文档回答使用 Utopia 过程中最常见的问题。如果你有未在本文档中解答的问题，欢迎提交 Issue。

---

## 目录

- [基础问题](#基础问题)
- [安装与配置](#安装与配置)
- [角色与角色卡](#角色与角色卡)
- [对话与交互](#对话与交互)
- [三引擎](#三引擎)
- [记忆系统](#记忆系统)
- [世界书](#世界书)
- [群聊](#群聊)
- [语音功能](#语音功能)
- [Token 预算](#token-预算)
- [命令行](#命令行)
- [插件系统](#插件系统)
- [数据与隐私](#数据与隐私)
- [故障排查](#故障排查)
- [开发相关](#开发相关)

---

## 基础问题

### Utopia 是什么？

Utopia 是一个运行在浏览器中的 AI 角色扮演 Agent。它把角色扮演中那些最难处理的部分（情感、身体、时间、记忆、规则）做好封装，让创作者专注于角色的性格和人设。

你可以把它理解成 **AI 角色扮演的 IDE**。

### Utopia 和 SillyTavern 有什么区别？

两者的**产品哲学**不同。

**SillyTavern** 是强大的前端，通过提示词工程让用户自己实现"角色状态"。它的上限更高（可以做到任何事情），但需要用户具备提示词工程能力。

**Utopia** 把"角色状态"内置为引擎，让用户不需要自己实现。它的下限更高（不需要任何配置就能让角色"活起来"），但复杂度集中在引擎层。

| 维度 | SillyTavern | Utopia |
|---|---|---|
| 定位 | 前端 + 提示词工程 | 完整的 Agent |
| 引擎 | 无（用户自行模拟） | 内置（三引擎） |
| 记忆 | 由用户配置 | 三层记忆（全文 + 向量 + 摘要） |
| 世界书 | 关键词匹配 | 条件 + 语义触发 + 规则组 |
| 插件 | 社区插件生态 | Worker 隔离 + 钩子 + 槽位 |
| 学习成本 | 高（需要理解提示词工程） | 中（需要理解引擎概念） |
| 部署门槛 | 中（Node.js 后端 + 前端） | 低（纯前端） |

### Utopia 和 Character.AI 有什么区别？

**Character.AI** 是消费级平台：零配置、开箱即用、无法控制底层。

**Utopia** 是工具：需要配置、提供完整控制权、完全本地运行。

Character.AI 关心"用户会不会用"，Utopia 关心"用户能不能做得更好"。

### 为什么没有官方角色库？

Utopia 是**工具**，不是**内容平台**。

角色库应该由用户自己创建和分享——这跟 GitHub 不会自己大规模产出开源项目是一个道理。官方只提供少量样板角色作为教程示例。

你可以导入 SillyTavern、Character.AI 或任何通用格式的角色卡，也可以自己创建。

### 为什么没有社区？

用户聚集的地方自然会出现社区（贴吧、B站、Discord、小红书）。

Utopia 的职责是保证**导出的东西别人能导入**、**做出来的工具别人能用**。社区的组织不需要官方介入。

### 为什么没有图片生成？

SillyTavern 集成了 A1111 和 ComfyUI。Utopia 目前专注于对话与角色行为，图片生成不在规划内。

如果你需要图片生成，可以先用其他工具生成素材，再上传为角色头像或聊天背景，或通过插件接入外部图像生成服务。

---

## 安装与配置

### 系统要求是什么？

- 现代浏览器（Chrome 90+ / Edge 90+ / Firefox 88+ / Safari 14+）
- 一个 AI 模型的 API Key（OpenAI / Anthropic / 或任意 OpenAI 兼容端点）
- 联网（除非你接入了本地模型）

无需 Node.js，无需构建工具，无需安装任何软件。

### 怎么运行？

Utopia 是纯前端应用，用任意静态服务器托管即可：

```bash
git clone https://github.com/404NotFoundObject/utopia.git
cd utopia
python -m http.server 8080
```

然后打开 `http://localhost:8080`。

**不要直接双击 `index.html`**。由于使用了 ES Modules 和 IndexedDB，`file://` 协议会导致部分功能失效。

### 支持哪些 API 厂商？

内置 9 家适配器：

- OpenAI
- Anthropic
- Google Gemini
- Cohere
- DeepSeek
- Mistral
- Groq
- Perplexity
- xAI (Grok)

此外，任何提供 OpenAI 兼容端点的服务都可以通过"OpenAI"适配器接入。

### 怎么接入本地模型？

本地推理引擎（Ollama / LM Studio / vLLM / llama.cpp 等）通常提供 OpenAI 兼容端点。

配置方式：

1. **设置 → API 配置**
2. 厂商选择 **OpenAI**
3. API Base URL 填入本地地址，例如：
   - Ollama: `http://localhost:11434/v1/chat/completions`
   - LM Studio: `http://localhost:1234/v1/chat/completions`
4. API Key 填任意非空值（本地服务通常不校验）
5. 模型名称填本地模型名（如 `qwen2.5:7b`）

> ⚠️ **注意浏览器的 CORS 限制**。如果本地服务未开启 CORS，需要在服务端配置 `Access-Control-Allow-Origin`。Ollama 需要设置 `OLLAMA_ORIGINS=*`。

### 为什么必须通过 HTTP 服务器访问？

Utopia 使用了：

- **ES Modules**（`<script type="module">`）：`file://` 协议下会被浏览器的同源策略阻止
- **IndexedDB**：部分浏览器在 `file://` 下禁用
- **Web Worker**（插件系统）：`file://` 下无法加载
- **fetch API**：调用 AI API 需要正确的 Origin

所以必须通过 HTTP 访问。这不是限制，是浏览器的安全策略。

### 需要配置 API Key 吗？

**创建角色和导入角色卡时**，如果 API Key 未配置或连接失败：

- **创建角色**：会警告"请先配置有效的 API 密钥"，无法创建
- **导入角色**：可以成功导入，但性格量化和动态开场白会使用默认值，导入后会提示你后续配置

**对话时**，必须有有效的 API Key，否则发送消息会失败。

---

## 角色与角色卡

### 支持哪些角色卡格式？

- **Utopia v3 / v3.1**（原生格式）
- **SillyTavern v2 / v3**（含 PNG 卡）
- **Character.AI**
- **通用 JSON 格式**

导入时系统会自动检测格式。

### 怎么导入角色？

**侧栏 → 导入** → 选择 `.json` 或 `.png` 文件。

PNG 卡会同时提取图片和嵌入的 JSON 数据。

### 怎么导出角色？

在角色列表项上点击 **导出** 按钮，选择目标格式：

- **PNG 卡**：生成带角色数据的图片，可重新导入
- **Utopia v3.1 / v3**：原生格式
- **SillyTavern v2 / v3**：兼容 ST
- **通用格式**：最简化

### 为什么导出的 PNG 卡在其他工具里无法识别？

Utopia 的 PNG 卡遵循 SillyTavern 的规范（tEXt 块中嵌入 `chara` 键），理论上应该兼容。如果遇到问题：

- 检查目标工具是否支持 PNG 卡
- 尝试导出为 JSON 格式
- 反馈问题时附上卡片的工具和错误信息

### 角色头像会被上传吗？

不会。头像以 Base64 形式存储在本地 IndexedDB 中，不会上传任何服务器。

### 为什么导入后没有性格参数？

如果你在**未配置 API Key** 的情况下导入角色，性格量化（六维参数）会使用默认值（均为 50），可能导致性格漂移或 OOC。

**补救方法**：配置 API 后，打开角色编辑表单，点击"重新量化性格"。

### 怎么让角色的性格更准确？

角色编辑表单中有"性格"字段——描述越详细，量化越准确。

例如：

- ❌ 差：`温柔`
- ✅ 好：`外表冷淡但内心温柔，说话简短含蓄，不擅长表达感情，但对亲近的人会展现细腻的一面`

性格描述会通过 AI 分析量化为 6 个维度（神经质 / 外向性 / 宜人性 / 开放性 / 尽责性 / 表达性），影响角色的情感反应、说话风格、事件响应强度。

---

## 对话与交互

### 消息支持哪些格式？

支持标准 Markdown：**粗体**、*斜体*、`行内代码`、代码块、列表、引用、链接、表格。

代码块带复制按钮。

### 怎么撤回消息？

- **桌面**：右键消息 → 撤回
- **移动端**：长按消息 → 撤回
- **命令行**：`/undo`（撤回最后一条用户消息和对应回复）

### 怎么重新生成回复？

- **桌面**：右键角色消息 → 重新生成
- **移动端**：长按角色消息 → 重新生成
- **命令行**：`/regen` 或 `/regenerate`

### 怎么编辑已发送的消息？

- **桌面**：右键用户消息 → 编辑
- **移动端**：长按用户消息 → 编辑

编辑后有"仅保存"和"保存并重新生成"两个选项。

### 怎么让角色朗读消息？

每条角色消息右侧有一个朗读按钮（音量图标）。点击后使用 Web Speech API 或你配置的 TTS 服务朗读。

也可以在**设置 → TTS**中配置默认音色，或为每个角色单独配置音色。

### 什么是推荐回复？

对角色消息右键 → 推荐回复，系统会根据对话上下文生成 3-4 条用户可能想说的话，点击后自动填入输入框。

### 角色为什么不回复？

可能原因：

1. **角色处于深睡或浅睡状态** —— 使用 `/wake` 或 `/wake force` 唤醒
2. **角色处于"困倦"或"恍惚"状态** —— 状态会影响回复质量，但不会完全不回复
3. **API 请求失败** —— 检查 API Key、余额、网络
4. **Token 超限** —— 打开 `/inspect` 查看 Token 预算
5. **发送中** —— 等待流式生成完成

查看控制台日志（F12）能快速定位问题。

### 角色回复质量差怎么办？

排查顺序：

1. **检查角色的系统提示词** —— 是否清晰定义了角色身份和行为
2. **检查性格量化结果** —— 打开角色编辑表单，查看六维参数是否合理
3. **检查世界书规则** —— 是否与角色设定冲突
4. **检查 Token 预算** —— 使用 `/inspect` 查看是否有内容被剪裁
5. **调整温度** —— 使用 `/temp 0.9` 尝试更高的随机性

### 怎么调温度等参数？

三种方式：

1. **设置面板**：设置 → 模型参数
2. **命令行临时覆盖**：`/temp 0.8`（当前会话有效，刷新后恢复）
3. **插件 API**：通过 `api.runtimeParams.setTempParam()`

---

## 三引擎

### 什么是三引擎？

Utopia 内置三个独立引擎，模拟角色的"生命状态"：

- **情感引擎**：六维情绪 + 五类需求 + 三类关系
- **身体状态引擎**：精力 / 睡意 / 健康 / 睡眠周期 / 疾病 / 受伤
- **时间引擎**：游戏时间基准

它们相互影响，共同决定角色的回复。

### 为什么角色会睡觉？

身体状态引擎会根据游戏时间自动触发睡眠。如果当前时间处于深夜（23:00-6:00），角色可能进入浅睡或深睡。

**相关设置**：

- 时间流速：设置 → 时间系统（可通过 `/time speed` 快速调整）
- 关闭引擎：设置 → 三大引擎控制

### 角色一直睡觉怎么办？

1. **唤醒**：发消息会尝试唤醒，但深睡时成功率低
2. **强制唤醒**：使用 `/wake force`
3. **调整时间流速**：使用 `/time speed 1` 放慢时间，避免频繁进入夜间
4. **调整游戏时间**：使用 `/time set 14:00` 直接跳到白天
5. **关闭引擎**：设置 → 三大引擎控制 → 关闭身体状态引擎

### 怎么让角色"生病"？

让角色的健康值低于 50，并且长时间不照顾，就有概率生病。也可以通过世界书规则或插件 API 直接触发。

### 可以关闭引擎吗？

可以。**设置 → 三大引擎控制**中，三个引擎可独立开关。

**关闭后果**：

- 关闭情感引擎：角色不会因交互产生情绪变化
- 关闭身体状态引擎：角色不会疲劳、不会生病、不会睡觉
- 关闭时间系统：游戏时间停止流动

如果你的角色卡本身已经通过提示词定义了状态，可以关闭引擎避免冲突。

### 时间流速是什么？

游戏时间与现实时间的比例。可选 1x / 2x / 4x / 8x / 24x / 48x。

**8x 流速**意味着 1 小时真实时间 = 8 小时游戏时间。

时间流速影响所有依赖"游戏时间"的机制：

- 冷落感知（多久未互动）
- 跨天感知（几天未联系）
- 主动对话触发
- 场景过期
- 情感 / 身体状态衰减

**推荐**：新用户使用 1x，熟悉后如需加快生活节奏可调高。

---

## 记忆系统

### 记忆是怎么工作的？

三层记忆：

1. **全文检索**（关键词匹配）：快速、精确、不消耗资源
2. **语义检索**（向量匹配）：理解意图，需要下载本地模型
3. **对话摘要**：AI 自动总结长对话，保持长期一致性

每次对话时，系统会从长期记忆中检索最相关的片段注入到上下文。

### 需要下载模型吗？

- **关键词检索**模式：不需要
- **语义检索 / 混合检索**模式：需要下载本地模型（80MB ~ 235MB）

下载入口：设置 → 长期记忆 → 下载模型。

推荐中文用户使用 `Xenova/paraphrase-multilingual-MiniLM-L12-v2`。

### 下载模型需要多久？

取决于网络速度：

- 80MB 模型：约 1-5 分钟
- 235MB 模型：约 3-15 分钟

模型会缓存到浏览器，下次启动不需要重新下载。

### 记忆会丢失吗？

正常情况下不会。记忆存储在 IndexedDB 中，除非：

- 用户手动清空浏览器数据
- 使用"重置所有数据"功能
- IndexedDB 存储空间不足

**记忆上限**：每个角色最多保留 500 条记忆（自动淘汰最旧的）。

### 记忆检索阈值是什么？

控制长期记忆召回的相似度门槛。独立于世界书语义阈值。

| 值 | 效果 |
|---|---|
| 0.4 | 宽松（召回多，可能包含不相关内容） |
| 0.55 | 平衡（推荐） |
| 0.7 | 严格（只召回高度相关的记忆） |

**调优建议**：

- AI 忘记之前聊过的内容 → 调低
- AI 插入不相关的记忆 → 调高

### 怎么查看现有记忆？

**命令行**：

- `/memory list` — 列出最近 10 条
- `/memory search <关键词>` — 搜索
- `/memory clear` — 清空（需确认）

也可以在**设置 → 长期记忆**中查看条目数。

---

## 世界书

### 世界书是什么？

一套动态规则注入系统。允许你在对话中根据任意条件自动向 AI 注入额外信息。

**核心公式**：当 X 发生时 → 自动告诉 AI 信息 Y

### 世界书有哪些规则类型？

- **条件触发**：根据条件表达式判断是否注入（关键词、数值、时间等）
- **常驻注入**：每轮对话都注入
- **语义触发**：用自然语言描述场景，系统自动匹配
- **粘性**：首次满足条件后持续注入

### 语义触发是什么？

用一句自然语言描述触发场景，系统根据用户消息的**语义相似度**自动匹配。

例如：

- 语义查询：`用户表达孤独、难过或需要安慰`
- 用户说"我今天有点难过" → 触发

**依赖**：设置 → 长期记忆中的语义模型。

### 为什么语义规则不触发？

排查顺序：

1. **模型是否下载**：设置 → 长期记忆 → 检查模型
2. **语义触发是否启用**：设置 → 世界书语义触发
3. **向量是否生成**：世界书界面 → 🧠 同步向量
4. **阈值是否过高**：尝试调低阈值（0.5 → 0.4）
5. **用 `/worldbook test <文本>` 测试**：查看具体分数

### 世界书语义阈值和记忆检索阈值有什么区别？

**独立配置，控制不同功能**：

- **世界书语义阈值**：控制世界书规则是否触发
- **记忆检索阈值**：控制长期记忆是否被召回

**调优时根据症状选择**：

| 症状 | 调整 |
|---|---|
| AI 忘记之前内容 | 调低记忆阈值 |
| AI 插入不相关记忆 | 调高记忆阈值 |
| 剧情规则老不触发 | 调低世界书阈值 |
| 剧情规则老误触发 | 调高世界书阈值 |

### 怎么调试世界书规则？

三种方式：

1. **编辑器内测试**：语义规则面板 → 🧪 测试匹配
2. **命令行测试**：`/worldbook test <文本>` — 显示所有规则的匹配分数
3. **对话后检查**：`/inspect` — 查看上一轮触发了哪些规则

---

## 群聊

### 群聊怎么工作？

多角色同屏对话。核心机制：

- **@ 提及**：`@角色名` 直接触发该角色回复
- **LLM 裁决**：无 @ 时由 LLM 选择最合适的角色回复
- **自主发言**：每 5 分钟轮询一次，角色按概率主动发言

### 群聊中 @ 角色的行为？

| 场景 | 行为 |
|---|---|
| 用户 @ 1 个角色 | 被 @ 的角色直接回复 |
| 用户 @ N 个角色 | 依次回复，间隔 800ms |
| 角色 A @ 角色 B | B 在 800ms 后回复 |
| B 又 @ A | 不再触发（防循环） |
| 用户未 @ 任何人 | LLM 裁决器选择 |

### 最多支持多少人？

表单限制 2-10 人。代码层无硬上限，但人数过多会导致：

- 每轮 LLM 调用成本增加
- 群聊摘要处理变慢
- 用户阅读负担加重

### 怎么管理群成员？

- **添加**：创建群组时选择成员
- **禁言 / 移出**：右键成员消息 → 对应选项（仅群主可见）

### 群聊摘要是什么？

每 10 条消息自动生成一次（可配置）。摘要会注入到每次对话的上下文中，帮助所有成员理解群聊历史。

查看：`/status` 命令输出群聊摘要。

---

## 语音功能

### TTS 支持哪些引擎？

- **Web Speech API**（Edge TTS）：免费，开箱即用
- **Kokoro**：本地 / 自托管，音质更好
- **自定义 OpenAI 兼容 API**：任何兼容 TTS 端点

### 怎么为角色设置专属音色？

角色编辑表单 → **语音配置**区域 → 选择音色、语速、音调。

### 语音通话怎么用？

- **发起**：单聊中输入 `/call`
- **接听**：来电弹窗 → 点击接听
- **挂断**：点击红色挂断按钮

通话采用状态机驱动的半双工模式：

- 角色说话 → 播放完成后进入监听
- 用户说话 → 识别完成后角色处理
- 交替进行，直到任一方挂断

### 为什么通话中无法使用？

检查：

1. **角色是否在睡觉** —— 深睡 / 浅睡 / 昏厥时无法通话，使用 `/wake force` 唤醒
2. **是否在群聊模式** —— 群聊不支持通话
3. **麦克风权限** —— 浏览器需要授权
4. **STT / TTS 配置** —— 设置面板中是否正确配置

### STT 支持哪些方式？

- **Web Speech API**：浏览器内置，仅 Chromium 系，实时流式
- **HTTP Whisper**：远程 API，全平台，需要配置端点

Web Speech 优先用于实时场景，Whisper 用于准确性要求高的场景。

### 长按快捷键是什么？

PC 端：按住 **Ctrl** 或 **Alt** 键说话，松开停止。

按住超过 200ms 才会触发录音，避免误触。

---

## Token 预算

### Token 预算是什么？

在发送 API 请求前，对上下文做 token 数量估算和裁剪，避免超出模型窗口。

### 分配比例是什么？

可用 token 按以下比例分配给不同内容：

- 系统提示：40%（动态注入部分，不含基础注入）
- 历史消息：40%
- 长期记忆：10%
- 对话摘要：10%

比例可在设置面板调整。

### 基础注入不占预算是什么意思？

**基础注入**包括：

- 角色的系统提示词
- 身份锁定
- 一致性约束
- 用户信息
- 时间上下文

这些内容**全量发送**，不参与裁剪。原因：

1. 它们是角色的核心定义，裁剪会导致人设漂移
2. 规模完全由用户控制，程序不替用户决定
3. 如果它们撑爆上下文窗口，API 会直接报错——这是用户输入问题

### 怎么查看预算使用情况？

**命令行**：`/inspect` — 显示上一轮的详细预算统计。

包括：

- 上下文窗口大小
- 可用预算
- 各类内容的预算 / 已用
- 是否发生裁剪 / 丢弃了哪些内容

### 三种剪裁策略是什么？

- **丢弃最低优先级**（默认）：丢弃优先级最低的整条消息
- **截断最长条目**：把最长的那条截断到预算内
- **摘要替换最旧**：用摘要替代最旧的历史消息

### 我怎么知道内容被剪裁了？

三种方式：

1. **`/inspect` 命令**：显示"⚠ 丢弃 N 条"和具体标签
2. **控制台日志**：`[TokenBudget]` 前缀的日志
3. **开发者监控**：`localStorage.setItem('utopia:dev-monitor', 'on')` 后查看 API 请求

如果发现关键内容被丢弃，调整：

- 增大"系统提示分配比例"
- 更换更大上下文的模型
- 精简角色的系统提示词
- 缩短对话历史

---

## 命令行

### 怎么使用命令？

在输入框中以 `/` 开头输入命令，例如 `/help`。

命令不会进入对话历史，执行结果通过 **"Utopia 控制台"** 气泡展示。

### 有哪些命令？

输入 `/help` 查看完整列表。

**分类**：

- **基础**：`/help` `/print` `/model` `/clear` `/reset`
- **时间控制**：`/time`（含 6 个子命令）
- **消息操作**：`/undo` `/regen`
- **角色相关**：`/switch` `/wake` `/status`
- **参数控制**：`/temp` `/max` `/fp` `/pp` `/topk` `/rp` `/reset_params`
- **检查工具**：`/inspect` `/worldbook test`
- **记忆管理**：`/memory`
- **系统**：`/stats` `/whoami` `/call` `/social`

### `/inject` 的提示词会在几轮对话中生效？

**仅一轮**（一次性）。发送下一条消息后自动清除，无论 AI 回复成功或失败。

如果需要提示词持续生效，请使用世界书规则。

### 怎么知道 `/inject` 是否注入了？

三种方式：

1. **发送前**：`/print` 查看"📝 待注入"字段
2. **发送后**：开启开发者监控，查看 `__engineMonitor.state.apiLog.at(-1).messages`
3. **Network 面板**：查看实际 API 请求的 messages 数组

### `/inspect` 输出太长怎么办？

`/inspect` 输出全部信息。如果只想看特定部分，可以用浏览器控制台的过滤功能（Ctrl+F）。

未来版本可能支持子命令（如 `/inspect budget` 只显示预算）。

### 命令可以在群聊中用吗？

可以。所有命令在群聊中同样有效。

- `/print` 自动识别群聊模式，输出群组信息
- `/status` 支持 `/status @成员名`
- `/call` 在群聊中会返回错误（群聊不支持通话）

---

## 插件系统

### 插件是什么？

通过独立 Worker 运行的扩展程序，无需修改核心源码。

**核心特性**：

- Worker 隔离：插件崩溃不影响主应用
- 双入口：`main.js`（逻辑）+ `ui.js`（界面）
- 权限声明：Manifest 显式声明所需权限
- 钩子系统：核心模块的方法自动支持 before / after / error 钩子
- UI 槽位：通过 `data-plugin-slot` 标注锚点

### 怎么安装插件？

**设置面板**：侧栏 → 插件 → 从 URL 或文件安装

**本地开发**：

1. 准备包含 `manifest.json` 和 `main.js` 的 zip 包
2. 通过插件管理界面安装

### 插件不能用裸模块导入？

**是的，这是设计边界。**

插件运行在 Blob URL 的 Worker 中，无法解析 importmap。平台已通过 `self.api` 提供所有核心能力的访问接口：

```js
// 在插件 main.js 中
export function setup(api, manifest) {
  const uuid = api.utils.generateUUID();
  const stores = await api.db.getStores();
  // 无需 import 任何外部包
}
```

如果插件使用裸模块（如 `import { unzipSync } from 'fflate'`），会加载失败，控制台会显示 `Failed to resolve module specifier`。

**这是插件的责任，不是平台的 BUG。**

### 插件权限有哪些？

Manifest 中声明所需权限，例如：

- `character:read` / `character:write`
- `chat:read` / `chat:write`
- `memory:read` / `memory:write`
- `worldbook:read` / `worldbook:write`
- `ui:inject`
- `hook:register`
- `event:subscribe`
- `api:call`

完整列表见插件编辑器中的权限说明，或参阅 [插件系统开发说明](docs/plugin-development.md)。

### 怎么调试插件？

浏览器控制台访问 `window.__utopiaPlugins`：

- `list()` — 列出所有已安装插件
- `running()` — 列出运行中的插件
- `hooks()` — 查看已注册的钩子
- `slots()` — 查看槽位注册表
- `enable(id)` / `disable(id)` / `reload(id)`

### 怎么卸载插件？

**侧栏 → 插件 → 卸载**（点击垃圾桶图标）

卸载会停止 Worker、清理钩子、事件订阅、UI 注入。

---

## 数据与隐私

### 数据存储在哪里？

所有数据存储在浏览器的 IndexedDB 中（数据库名 `UtopiaDB`）。

**包括**：角色、会话、消息、记忆、世界书规则、群组、朋友圈动态、设置。

**不存储**：任何上传到服务器的数据（API Key 存储在本地 IndexedDB 中）。

### 会上传到服务器吗？

不会。Utopia 是纯前端应用，没有后端服务。

**唯一的外部请求**：

- 调用 AI API（你的消息内容会发送到 API 厂商）
- 下载语义模型（从 Hugging Face）
- 获取网络时间（用于时间同步，可关闭）

### 怎么备份数据？

**官方方式**：目前没有一键备份功能。

**替代方案**：

1. 导出所有角色卡（JSON / PNG）
2. 导出世界书（JSON）
3. 导出 API Key 等设置（手动记录）

**高级用户**：使用浏览器开发者工具 → Application → IndexedDB → 导出 `UtopiaDB`。

### 怎么迁移到另一台设备？

**方式 1：手动备份 → 手动恢复**

- 导出角色卡、世界书
- 在新设备上导入

**方式 2：浏览器同步**

- 部分浏览器支持 IndexedDB 同步（如 Chrome 的 Profile 同步）
- 但兼容性不稳定，不推荐

**方式 3：导出数据库文件（高级）**

- 从 IndexedDB 导出 → 复制到新设备 → 导入

### 怎么重置所有数据？

**设置 → 危险操作 → 重置所有数据**

会清空：角色、会话、记忆、世界书、插件、自定义主题。

**不可恢复。** 建议提前备份。

### 浏览器存储空间不足怎么办？

IndexedDB 通常有数百 MB 到数 GB 的配额。如果提示空间不足：

1. **清理旧记忆**：`/memory clear`
2. **清理旧会话**：删除不用的角色
3. **清理旧朋友圈**：`/social clear`
4. **检查浏览器配额**：浏览器开发者工具 → Application → Storage

### API Key 会被泄露吗？

API Key 存储在本地 IndexedDB 中，**不会上传到任何服务器**。

但需要注意：

- **浏览器扩展**可能读取 IndexedDB（如果扩展权限过高）
- **共享浏览器**环境下，其他用户可以读取（未设置密码保护）
- **导出的设置**（如果有）可能包含 API Key

如果对安全有较高要求，建议：

- 使用专用浏览器 Profile
- 不在公共电脑上保存 API Key

---

## 故障排查

### 应用打不开 / 白屏

**检查顺序**：

1. **F12 打开控制台**，看是否有红色错误
2. **确认通过 HTTP 服务器访问**（不是 `file://`）
3. **清除浏览器缓存**后重试
4. **尝试无痕模式**排除扩展干扰
5. **换个浏览器**排除浏览器兼容问题

**常见错误**：

- `Failed to load module script` → 未通过 HTTP 访问
- `IndexedDB access denied` → 隐私模式或浏览器设置限制
- `Cannot read properties of null` → 数据损坏，尝试"重置所有数据"

### IndexedDB 数据库损坏

应用启动时会检测数据库状态。如果显示错误对话框：

- **删除并重建**：清空所有数据（不可恢复）
- **取消**：应用无法启动

如果不想丢失数据，在删除前尝试：

1. 手动打开 IndexedDB 导出数据（浏览器开发者工具）
2. 修复后重新导入

### 消息发送失败

**排查顺序**：

1. **控制台日志**：查看具体错误信息
2. **API 配置**：`/model` 确认配置
3. **网络连通性**：`/print` 或访问 `/stats` 检查
4. **余额**：API 厂商账户是否有余额
5. **模型名称**：是否拼写正确
6. **CORS**：如果是本地模型，检查 CORS 配置

**常见错误**：

| 错误 | 原因 |
|---|---|
| `API Key 未配置` | 设置面板未填写 Key |
| `401 Unauthorized` | Key 无效或过期 |
| `429 Too Many Requests` | 速率限制，等待或升级 |
| `context_length_exceeded` | 上下文超模型窗口，精简提示词 |
| `CORS policy` | 本地服务未开启 CORS |

### 角色不回复 / 回复空白

**可能原因**：

1. **角色在睡觉** → `/wake force`
2. **API 超时** → 检查网络
3. **Token 超限** → `/inspect` 查看预算
4. **模型返回空** → 尝试提高温度 `/temp 0.9`

**检查方式**：F12 → Network → 查看 API 请求的响应。

### 语义检索不工作

**排查顺序**：

1. **模型是否下载**：设置 → 长期记忆 → 检查模型
2. **检索模式是否正确**：设置 → 长期记忆 → 检索模式（应为"语义"或"混合"）
3. **是否在后台初始化**：首次启动需要 30-60 秒加载模型
4. **控制台日志**：搜索 `[Memory]` 前缀的日志

### 语音识别不工作

**排查**：

1. **浏览器支持**：Web Speech API 仅 Chromium 系支持
2. **麦克风权限**：浏览器需要授权
3. **HTTPS 要求**：部分浏览器要求 HTTPS 才能访问麦克风（`localhost` 例外）
4. **STT 配置**：设置 → STT，检查提供商和语言

### 数据丢失

**可能原因**：

1. **清空浏览器数据**（Cookie / 站点数据）
2. **切换浏览器 / Profile**
3. **IndexedDB 被系统清理**（存储空间不足时）
4. **隐私模式下关闭标签页**

**预防措施**：

- 定期导出重要角色卡
- 使用固定浏览器 Profile
- 避免在隐私模式下使用

### 遇到 bug 怎么报告？

提交 Issue 时请附上：

1. **浏览器和版本**：如 `Chrome 120.0.6099.71`
2. **操作系统**：如 `Windows 11 / macOS 14.2 / Ubuntu 22.04`
3. **复现步骤**：1、2、3…… 越详细越好
4. **控制台日志**：F12 → Console 面板的完整输出
5. **截图**：如果涉及 UI 问题
6. **预期行为 vs 实际行为**

如果开启了开发者监控，附上相关日志会大大加快定位速度。

---

## 开发相关

### 怎么开发插件？

**基本结构**：

```
my-plugin/
├── manifest.json    # 插件元数据
├── main.js          # Worker 入口（逻辑）
└── ui.js            # UI 入口（可选）
```

**manifest.json 示例**：

```json
{
  "id": "com.example.my-plugin",
  "name": "My Plugin",
  "version": "1.0.0",
  "description": "A demo plugin",
  "author": "Your Name",
  "main": "main.js",
  "ui": "ui.js",
  "permissions": ["character:read", "chat:write"]
}
```

**main.js 示例**：

```js
export function setup(api, manifest) {
  console.log('Plugin loaded:', manifest.name);

  // 注册钩子
  api.hooks.register('chat.sendMessage:before', (ctx) => {
    console.log('Before sending:', ctx.args);
  });

  // 注册命令
  api.commandEngine.registerCommand('hello', async (args, context) => {
    return { banner: 'Hello from plugin!' };
  });

  return function teardown() {
    // 清理资源
  };
}
```

完整开发说明见 [插件系统开发说明](docs/plugin-development.md)。

### 怎么修改主题？

- **内置主题**：设置 → 主题
- **自定义主题**：设置 → 主题 → 🎨 主题制作器

可以调整颜色、圆角、阴影，支持实时预览和导入导出。

### 怎么贡献代码？

1. Fork 仓库
2. 创建分支：`git checkout -b feature/your-feature`
3. 提交改动：`git commit -m "feat: add xxx"`
4. 推送：`git push origin feature/your-feature`
5. 提交 Pull Request

**规范**：

- 一个 PR 只做一件事
- 提交信息用清晰的动词开头（`fix:` / `feat:` / `docs:` / `refactor:`）
- 涉及核心逻辑的改动请附上测试说明

详见 [CONTRIBUTING.md](CONTRIBUTING.md)。

### 项目使用什么许可证？

**Apache License 2.0**。

你可以：

- 商用、闭源、修改、分发
- 无需开源你的修改（但需保留版权声明）

你不能：

- 用 "Utopia" 商标冒充官方项目

详见 [LICENSE](LICENSE)、[NOTICE](NOTICE) 和 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

### 开发者监控怎么用？

**启用**（一次性设置）：

```js
localStorage.setItem('utopia:dev-monitor', 'on');
location.reload();
```

**关闭**：

```js
localStorage.removeItem('utopia:dev-monitor');
location.reload();
```

**实时监控**：启用后，控制台会打印引擎事件和 API 请求。

**API 命令**：

- `__engineMonitor.report()` — 事件统计
- `__engineMonitor.snapshot()` — 记录状态快照
- `__engineMonitor.diff()` — 对比快照变化
- `__engineMonitor.stop()` — 停止监控

**典型用途**：

```js
// 验证情感引擎副作用
await __engineMonitor.snapshot();
// 发送"你真漂亮"
await __engineMonitor.diff();
// 应看到 valence / affection 上升

// 检查 API 注入内容
__engineMonitor.state.apiLog.at(-1).messages
```

### 全局调试变量有哪些？

- `window.__lastBudgetStats` — 上一轮 Token 预算统计
- `window.__lastSemanticTrace` — 上一轮语义匹配轨迹
- `window.__utopiaApi` — 插件 API 聚合对象
- `window.__utopiaPlugins` — 插件调试 API
- `window.__engineMonitor` — 开发者监控（需启用）

---

## 更多帮助

- **应用内指南**：设置 → 📖 使用指南
- **世界书教程**：侧栏 → 世界书 → 📖 教程
- **插件开发说明**：[docs/plugin-development.md](docs/plugin-development.md)
- **命令行帮助**：`/help`
- **提交 Issue**：https://github.com/404NotFoundObject/utopia/issues

---

**Utopia** · 永远的理想国